export const COLUMNS_KEYS = {
  horizontalColumns: (frameId: string) => [
    "overlay",
    frameId,
    "horizontal-columns",
  ],
  verticalColumns: (frameId: string) => [
    "overlay",
    frameId,
    "vertical-columns",
  ],
};
