import { IRuler } from './IRuler';
import { Color } from '../../utils/Color';

const ruler: IRuler = {
  id: '',
  x: 250,
  y: 250,
  width: 400,
  height: 400,
  color: Color.ORANGE
};

export default ruler;
